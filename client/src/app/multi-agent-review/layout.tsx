import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Multi-Agent Review · DevDigest",
};

export default function MultiAgentReviewLayout({ children }: { children: React.ReactNode }) {
  return children;
}
