/* Re-exports shared eval styles so existing relative imports within this
   folder continue to resolve while the canonical source lives in
   @/components/evals/styles. */
export { s } from "@/components/evals/styles";
