# client/docs/

Deep dives that don't fit in `client/README.md`: page-level state machines,
shortcut chart, design tokens, recharts/mermaid recipes, theme cookbook.

`client/README.md` stays the high-level entry point. Promote a topic here
once it grows past ~30 lines or wants its own diagram. Link new files from
`client/CLAUDE.md` via the `Read … when …` block.
