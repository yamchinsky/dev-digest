# reviewer-core/docs/

Deep dives that don't fit in `reviewer-core/README.md`: prompt-assembly
internals, grounding-gate reasoning, INJECTION_GUARD threat model,
map-reduce orchestration, structured-output repair strategy.

`reviewer-core/README.md` stays the pipeline overview. Promote a topic here
once it grows past ~30 lines or wants its own diagram. Link new files from
`reviewer-core/CLAUDE.md` via the `Read … when …` block.
